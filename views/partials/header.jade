nav.navbar.navbar-absolute.navbar-fixed-top(role='navigation')
	//nav.navbar.navbar-inverse.navbar-fixed-top(role='navigation')
	.container-fluid
		.navbar-header
			button.navbar-toggle(type='button', data-toggle='collapse')
				span.sr-only Toggle navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href='/') eShuri Platform 
		.collapse.navbar-collapse
			ul.nav.navbar-nav.navbar-right
				li.dropdown
					if access
						a.btn-simple.btn.label.btn-info.dropdown-toggle(data-toggle="dropdown")
							i.material-icons person
							| <strong>{{'#{pic_name}'|| 'Account'|uppercase}}</strong>
					if !access
						a.btn-simple.btn.label.btn-info.dropdown-toggle(data-toggle="dropdown")
							i.material-icons person
							| New Account
					ul.dropdown-menu
						if access
							li
								a(href="/user.settings")
									i.material-icons settings
									|	 Change your settings
						if !access
							li.divider
							li
								a(href="/user.signup")
									i.material-icons person
									|  New account
							li.divider
							li
								a(href="/user.signin")
									i.material-icons person
									|		Login
						if access
							li.divider
							li
								a(href="/user.logout")
									i.material-icons face
									|	Logout